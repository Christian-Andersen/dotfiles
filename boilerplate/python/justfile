set dotenv-load

alias c := check
alias r := run
alias ci := continuous_integration

[private]
default:
    @just --choose

check:
    uvx prek@latest run --all-files

run *args:
    uv run -- main.py {{ args }}

audit:
    uv audit

continuous_integration: audit check
